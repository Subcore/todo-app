# Переменные окружения для todo-api
# Взяты из docker-compose.yaml → services.api.environment
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER={{ db_user }}
DB_PASSWORD={{ db_password }}
DB_NAME={{ db_name }}
DB_SSLMODE=disable
ENABLE_SWAGGER=true
PORT={{ app_port }}
