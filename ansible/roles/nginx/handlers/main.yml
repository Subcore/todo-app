---
# Reload nginx — applies the new config without dropping connections.
# reload (not restart) is the standard practice for nginx.
- name: reload nginx
  service:
    name: nginx
    state: reloaded
